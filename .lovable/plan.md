

## Plan: Add Meta Pixel to `index.html`

Insert the provided Meta Pixel code into the `<head>` section of `index.html`, right before the closing `</head>` tag.

### Changes

**`index.html`** — Add the Meta Pixel script block (pixel ID `904596982463950`) after the existing `<meta>` tags and before `</head>`.

